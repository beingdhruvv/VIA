# VIA — Agent Context & Handoff
> Single source of truth for agents and humans.
> Read before touching anything. Update before leaving.

---

# NAVIGATION

| Goal                              | Go to              |
|-----------------------------------|--------------------|
| Resume quickly / what's happening | HANDOFF STATE      |
| Which file owns what              | FILE MAP           |
| Directory layout + versions       | WORKING DIRECTORY  |
| DB / API paths                    | DATA & PATHS       |
| Build and ship / 502 recovery     | BUILD & DEPLOY     |
| Rules that must never break       | HARD RULES         |
| Why we made a choice              | DECISION LOG       |
| What has been tested              | VALIDATION LOG     |
| Open tasks                        | BACKLOG            |
| Server / infra details            | SERVER             |

---

# AGENT PROTOCOL

## Session START — do in order
1. Read HANDOFF STATE. Understand status, blockers, next step.
2. Run: git log -5 --oneline to confirm current commit.
3. Check FILE MAP for areas you will touch.
4. Never trust stale notes — always verify the actual file first.

## Session END — mandatory before closing
1. Rewrite HANDOFF STATE: date+time, summary, blockers, next step.
2. Update WORKING DIRECTORY if files were added/renamed/deleted.
3. Append a row to DECISION LOG for every non-obvious choice made.
4. Append a row to VALIDATION LOG if build ran or tests done.
5. Update CONTEXT FILE LOG at the bottom.

## What agents must never do
- Never commit secrets, API keys, or credentials.
- Never write AI/agent/IDE name in commit messages or code comments.
- Never mark build as passing without actually running: npm run build
- Never leave HANDOFF STATE blank or with a stale date.

---

# HANDOFF STATE

| Field           | Value                                                              |
|-----------------|--------------------------------------------------------------------|
| Last updated    | 2026-05-11 09:50 IST (session: Private Uploads + Build Verify)     |
| Last updated by | Codex                                                              |
| Build status    | PASSING (`npm.cmd run build`, 39 routes; `npx.cmd tsc --noEmit`)   |
| Active branch   | main                                                                |
| Last commit     | (update when committing)                                            |
| Blockers        | None                                                                |
| Next step       | Deploy after owner review; refresh IDE TS server if stale diagnostics remain |

## What was done this session
- **Auth / Firebase (prod):** `FIREBASE_WEB_*` in `.env.production`; `lib/firebase-server-config.ts` merges `.env.production` into `process.env` once; **`proxy.ts`** — allow **`/api/config/*`** without session.
- **Memories (Google Photos style):** Implemented `/memories` gallery and `/api/memories` upload handler. 200MB quota per user. Grouping by date, trip-specific filtering, and full-screen preview.
- **Profile & Avatar:** Added `/api/users/avatar` for persistent profile picture uploads. Fixed avatar display across the app. Removed redundant "Critical Actions" card.
- **Trip Enhancements:** Added `DELETE` support for trips. Added `shareMemories` toggle. Collaborators can now be added during trip creation via email invitation.
- **Explore Tab:** Redesigned `ExploreSwiper` with brutalist metadata overlay, corner controls, and dedicated "Save" button. Fixed Next.js 15 async params in API routes.
- **Private uploads:** Added authenticated `/api/uploads/[...path]`, moved new memory/avatar uploads to `storage/uploads`, preserved legacy `/public/uploads` fallback, and redirects old `/uploads/*` URLs through the API route.
- **Images & Profile:** Profile route builds as dynamic/authenticated. Things-to-do images now use per-activity image queries instead of reusing the same city hero.

## What reliably works
- Prisma + seed (SQLite dev / PostgreSQL prod)
- `npm run build` when CI env clean
- GitHub Actions → droplet deploy (`deploy.sh`)
- Google Sign-In when `FIREBASE_WEB_*` set + **Authorized domains** in Firebase + **`/api/config/*` public in `proxy.ts`**
- Dev server at http://localhost:3000

## Deployment
- Production URL, SSH, keys: **`LOCAL_OPERATIONS.md`** (gitignored; copy from `LOCAL_OPERATIONS.md.template`).
- App on server: `/var/www/via`; deploy: push `main` → Actions → `infra/server/deploy.sh`.
- **Production Firebase:** set **`FIREBASE_WEB_*`** in `/var/www/via/.env.production` (same values as Firebase web SDK config); optional `NEXT_PUBLIC_FIREBASE_*` if rebuilding with keys baked in.

## Known issues / caveats
- Prefer passphrase-free deploy key for CI.
- NextAuth `serverActions.allowedOrigins` experimental warning (non-breaking).
- Prisma SQLite uses String enums; prod uses `schema.production.prisma`.

---

# PROJECT SNAPSHOT

Purpose: VIA — personalized multi-city travel planning web app for Odoo Hackathon 2026 (Team StormLabs)

Stack:
- Frontend: Next.js 16.2.6 (App Router), TypeScript, Tailwind CSS v4
- Auth: NextAuth v5 beta (Credentials + JWT) + **Firebase Google** (client SDK; config via `FIREBASE_WEB_*` / bootstrap / `/api/config/firebase`)
- DB (local): SQLite via Prisma 5
- DB (production): PostgreSQL via Prisma 5
- UI: Custom Tailwind components + Radix UI headless primitives
- Charts: Recharts
- Maps: react-leaflet + Leaflet
- Drag/Drop: dnd-kit
- Forms: React Hook Form + Zod
- State: Zustand
- Animations: Framer Motion
- Icons: Lucide React
- Hosting target: DigitalOcean Droplet (Ubuntu 24.04, Nginx + PM2)

---

# WORKING DIRECTORY

```
<repo-root>/
├── app/                        # All Next.js pages + API routes
│   ├── api/
│   │   ├── auth/[...nextauth]/ # NextAuth handler
│   │   ├── auth/signup/        # POST create user
│   │   ├── trips/              # GET all, POST create
│   │   ├── trips/[id]/         # GET, PATCH, DELETE trip
│   │   ├── trips/[id]/stops/   # GET, POST stops
│   │   ├── trips/[id]/expenses/# GET, POST expenses
│   │   ├── trips/[id]/packing/ # GET, POST packing items
│   │   ├── trips/[id]/notes/   # GET, POST notes
│   │   ├── trips/[id]/share/   # POST generate public slug
│   │   ├── stops/[id]/         # PATCH, DELETE stop
│   │   ├── stops/[id]/activities/ # GET, POST (toggle) activities
│   │   ├── packing/[id]/       # PATCH, DELETE item
│   │   ├── cities/search/      # GET city search (Prisma City table only)
│   │   ├── cities/explore/     # GET explore deck (auth)
│   │   ├── config/firebase/    # GET public Firebase web config (no auth)
│   │   ├── cities/[id]/activities/ # GET activities for city
│   │   ├── weather/            # GET Open-Meteo weather
│   │   ├── memories/           # GET all, POST upload, DELETE
│   │   ├── users/              # GET, PATCH user
│   │   └── users/avatar/       # POST avatar upload
│   ├── auth/login/             # ✅ DONE
│   ├── auth/signup/            # ✅ DONE
│   ├── auth/forgot-password/   # ✅ DONE
│   ├── dashboard/              # ✅ DONE
│   ├── trips/                  # ✅ DONE
│   ├── trips/new/              # ✅ DONE
│   ├── trips/[id]/             # ✅ DONE (itinerary view)
│   ├── trips/[id]/builder/     # ✅ DONE
│   ├── trips/[id]/budget/      # ✅ DONE
│   ├── trips/[id]/packing/     # ✅ DONE
│   ├── trips/[id]/notes/       # ✅ DONE
│   ├── cities/                 # ✅ DONE
│   ├── cities/[id]/            # ✅ DONE
│   ├── explore/                # ✅ Swipe / taste (auth)
│   ├── trip/[slug]/            # ✅ DONE (public view)
│   ├── profile/                # ✅ DONE
│   ├── admin/                  # ✅ DONE
│   ├── page.tsx                # ✅ Landing page
│   ├── layout.tsx              # ✅ Root layout + fonts
│   └── globals.css             # ✅ VIA design tokens
├── components/
│   ├── ui/                     # ✅ Button, Input, Textarea, Select, Badge,
│   │                           #    Card, Modal, Sheet, Skeleton, Avatar,
│   │                           #    ProgressBar, Tabs
│   ├── layout/                 # ✅ Sidebar, Navbar, BottomNav, AppShell, PageHeader
│   ├── trip/                   # ⏳ TripCard, StopCard, ActivityCard etc.
│   └── forms/                  # ✅ LoginForm, SignupForm (CreateTripForm etc. pending)
├── lib/
│   ├── prisma.ts               # ✅ Singleton Prisma client
│   ├── auth.ts                 # ✅ NextAuth config
│   ├── utils.ts                # ✅ cn, formatDate, getCityImageUrl, etc.
│   ├── place-images.ts         # ✅ Wikimedia city image map + fallback
│   ├── firebase.ts             # ✅ Firebase web SDK init + runtime override
│   ├── firebase-server-config.ts # ✅ FIREBASE_WEB_* + merge .env.production
│   ├── useFirebaseReady.ts     # ✅ Client: bootstrap + /api/config/firebase
│   └── app-version.ts          # ✅ NEXT_PUBLIC_APP_VERSION (build label)
├── store/
│   └── index.ts                # ✅ Zustand UI store
├── types/
│   ├── index.ts                # ✅ All shared types
│   └── next-auth.d.ts          # ✅ Session type extension
├── prisma/
│   ├── schema.prisma           # ✅ 9 models (SQLite compatible)
│   ├── seed.ts                 # ✅ 57 cities + 295 activities
│   ├── dev.db                  # ✅ SQLite DB (local, gitignored)
│   └── migrations/             # ✅ 20260510_init
├── proxy.ts                    # ✅ Auth gate (Next 16); public: /, /auth, /trip, /api/auth, /api/config, /api/users
├── next.config.ts              # ✅ Image domains configured
├── .env                        # ✅ DATABASE_URL=file:./prisma/dev.db
└── .env.local                  # ✅ NextAuth secrets + GeoDB key placeholder
```

File status:
| Path                          | Status      | Last edited  | Notes                                         |
|-------------------------------|-------------|--------------|-----------------------------------------------|
| prisma/schema.prisma          | active      | 2026-05-10   | SQLite for dev; switch to postgresql for prod |
| prisma/seed.ts                | active      | 2026-05-10   | 57 cities, 295 activities                     |
| app/globals.css               | active      | 2026-05-10   | VIA design tokens via @theme                  |
| app/layout.tsx                | active      | 2026-05-10   | Space Grotesk + Inter + IBM Plex Mono         |
| lib/auth.ts                   | active      | 2026-05-10   | NextAuth v5; redirect uses AUTH_URL / NEXTAUTH_URL |
| proxy.ts                      | active      | 2026-05-10   | Public: /api/config/* for Firebase on login     |
| components/ui/*               | active      | 2026-05-10   | All base UI components                        |
| components/layout/*           | active      | 2026-05-10   | Sidebar, Navbar, BottomNav, AppShell          |

---

# FILE MAP

| Feature / Area        | Primary file(s)                                   | Notes                               |
|-----------------------|---------------------------------------------------|-------------------------------------|
| App entry / routing   | app/page.tsx, proxy.ts                            | Landing + auth guard                |
| Root layout           | app/layout.tsx, app/globals.css                   | Fonts, design tokens                |
| Auth pages            | app/auth/*/page.tsx                               | Login/signup `force-dynamic` + Firebase bootstrap |
| Auth forms            | components/forms/LoginForm.tsx, SignupForm.tsx     | RHF + Zod; `useFirebaseReady`                     |
| Auth gate             | proxy.ts                                          | Next 16 middleware; **must** allow `/api/config/*` unauthenticated |
| Firebase server       | lib/firebase-server-config.ts, app/api/config/firebase/ | `FIREBASE_WEB_*` + `.env.production` merge |
| Firebase client       | lib/firebase.ts, lib/useFirebaseReady.ts          | Runtime init + Google                           |
| Auth API              | lib/auth.ts, app/api/auth/*/                      | NextAuth + bcryptjs                 |
| DB client             | lib/prisma.ts                                     | Singleton Prisma 5                  |
| DB schema             | prisma/schema.prisma                              | 9 models                            |
| DB seed               | prisma/seed.ts                                    | Cities + activities                 |
| UI components         | components/ui/*                                   | All custom Tailwind                 |
| Layout components     | components/layout/*                               | Sidebar, AppShell etc.              |
| Trip components       | components/trip/*                                 | TripCard, StopCard etc. (pending)   |
| Explore (swipe)       | app/explore/page.tsx, components/explore/ExploreSwiper.tsx | Auth; `place-images` heroes      |
| Shared types          | types/index.ts                                    | TripCard, StopWithCity etc.         |
| API: trips            | app/api/trips/*/route.ts                          | Full CRUD                           |
| API: stops            | app/api/stops/[id]/route.ts                       | PATCH, DELETE + reorder             |
| API: expenses         | app/api/trips/[id]/expenses/route.ts              | GET, POST, DELETE                   |
| API: packing          | app/api/trips/[id]/packing/route.ts               | GET, POST, bulk                     |
| API: notes            | app/api/trips/[id]/notes/route.ts                 | GET, POST                           |
| API: city search      | app/api/cities/search/route.ts                    | Prisma `City` only (no GeoDB)       |
| API: config           | app/api/config/firebase/route.ts                  | Public Firebase web fields          |
| Utility functions     | lib/utils.ts, lib/place-images.ts                 | City/activity image URLs (Wikimedia) |
| App build label       | lib/app-version.ts, next.config.ts `env`          | `NEXT_PUBLIC_APP_VERSION`            |

---

# SERVER

| Field           | Value                                        |
|-----------------|----------------------------------------------|
| Notes           | Put real IPs, domain, SSH labels in `LOCAL_OPERATIONS.md` (gitignored). |
| Provider        | (your host)                                  |
| App directory   | `/var/www/via`                               |
| Runtime         | Node.js 22, PostgreSQL, Nginx, PM2           |

Server setup (once SSH works):
1. Add deploy public key to server `authorized_keys`
2. Clone repo under `/var/www/via`
3. Run `infra/server/bootstrap-ubuntu-24-04.sh`
4. Create `.env.production` from `.env.production.example`
5. Run `infra/server/deploy.sh`
6. Verify PM2, Nginx, app URL, and logs

---

# DATA & PATHS

Local (SQLite dev):
- DATABASE_URL = file:./prisma/dev.db
- DB file: `./prisma/dev.db` (repo-relative path)

Production target (PostgreSQL):
- DATABASE_URL = postgresql://via_user:PASSWORD@localhost:5432/via_db
- Must update prisma/schema.prisma: provider = "postgresql"
- Must re-add @db.Date, @db.Text, and real enum types

API routes:
| Endpoint                          | Method      | Auth | Purpose                          |
|-----------------------------------|-------------|------|----------------------------------|
| /api/auth/signup                  | POST        | No   | Create user account              |
| /api/auth/[...nextauth]           | GET/POST    | No   | NextAuth handler                 |
| /api/trips                        | GET, POST   | Yes  | List/create trips                |
| /api/trips/[id]                   | GET/PATCH/DELETE | Yes | Single trip CRUD             |
| /api/trips/[id]/stops             | GET, POST   | Yes  | Add/list stops                   |
| /api/stops/[id]                   | PATCH/DELETE| Yes  | Update/delete stop               |
| /api/stops/[id]/activities        | GET, POST   | Yes  | Toggle activity on stop          |
| /api/trips/[id]/expenses          | GET/POST/DELETE | Yes | Expense management           |
| /api/trips/[id]/packing           | GET, POST   | Yes  | Packing checklist                |
| /api/packing/[id]                 | PATCH/DELETE| Yes  | Toggle/delete packing item       |
| /api/trips/[id]/notes             | GET, POST   | Yes  | Trip notes                       |
| /api/trips/[id]/share             | POST        | Yes  | Generate public slug             |
| /api/cities/search                | GET         | No   | City search (Prisma City table)  |
| /api/cities/[id]/activities       | GET         | No   | Activities for a city            |
| /api/weather                      | GET         | No   | Open-Meteo weather               |

External APIs:
| API              | URL                                              | Key required |
|------------------|--------------------------------------------------|--------------|
| GeoDB Cities     | https://wft-geo-db.p.rapidapi.com (distance route only; search does not use) | Optional `GEODB_API_KEY` |
| Open-Meteo       | https://api.open-meteo.com/v1/forecast           | No           |
| Unsplash Source  | https://source.unsplash.com/800x600/?{query}     | No           |
| ExchangeRate     | https://open.er-api.com/v6/latest/INR            | No           |

---

# BUILD & DEPLOY

| Command                    | What it does                            |
|----------------------------|-----------------------------------------|
| npm run dev                | Dev server at http://localhost:3000     |
| npm run build              | Production build (tsc + Next.js)        |
| npm run start              | Start production server                 |
| **Deploy build id**        | CI sets `DEPLOY_VERSION` → `deploy.sh` exports `NEXT_PUBLIC_APP_VERSION` before `npm run build`. Local dev uses `package.json` `version` via `next.config.ts`. |
| npm run db:generate        | npx prisma generate                     |
| npm run db:migrate         | npx prisma migrate dev (dev only)       |
| npm run db:seed            | tsx prisma/seed.ts                      |
| npm run db:push            | npx prisma db push (quick schema push)  |

## Deploy tips — 502 / stuck deploys (droplet)

What went wrong once: **tracked files edited on the server** under `/var/www/via` (common: `deploy.sh`, `package.json`). A deploy that ran **`git checkout` (or similar) before `git reset --hard`** could **fail** on a dirty tree; with `set -e` the script **exited early** → **no clean build/reload** → **502** until someone fixed the box by hand.

Tips (do not skip):
- **Do not** hot-edit tracked files on the droplet. Commit locally, push `main`, let GitHub Actions deploy.
- Before blaming the app: **`cd /var/www/via && git status`**. If anything under tracked paths is modified, that is a red flag for failed deploys.
- **Current fix in repo:** `git fetch` → **`git reset --hard origin/<branch>`** (immediately; no checkout-before-reset) → **`git clean -fd -e .env.production`**. Same reset+clean runs from **CI SSH before `deploy.sh`** (see `.github/workflows/deploy.yml`, `infra/server/deploy.sh` comments).
- **502 triage on server:** `pm2 logs <app>` (often `via`), `curl -fsS http://127.0.0.1:3000/api/health`, Nginx error log / upstream. If the tree was dirty, **`git fetch origin main && git reset --hard origin/main`** (then re-run deploy or push again) usually unblocks.
- **Never lose prod env:** `git clean` must keep **`.env.production`** — the script uses `-e .env.production` on purpose.

Dev server start command (set secrets locally — do not paste real keys into this repo file):
```powershell
cd <YOUR_REPO_ROOT>
$env:DATABASE_URL="file:./prisma/dev.db"
$env:NEXTAUTH_SECRET="<openssl rand -base64 32 output>"
$env:AUTH_SECRET="<same as NEXTAUTH_SECRET>"
$env:NEXTAUTH_URL="http://localhost:3000"
npm run dev
```

Pre-ship checklist:
- [x] npm run build passes (0 errors) — confirmed 2026-05-10
- [ ] All Tier 1 pages work end-to-end
- [ ] No console errors in production build
- [ ] Responsive at 375px and 768px
- [ ] SSH to droplet resolved + server setup done
- [ ] `.env.production` on droplet: `DATABASE_URL`, `NEXTAUTH_*`, **`FIREBASE_WEB_*`** (Google), optional `GEODB_API_KEY`

---

# HARD RULES

## Never do
- Never use gradient backgrounds in UI
- Never use border-radius > 4px (except avatars = 50%)
- Never use emojis in UI
- Never use Bootstrap, MUI, Chakra, Ant Design
- Never use blurry/soft shadows — only 3px offset brutalist shadow
- Never hardcode city/activity data in components — always from DB
- Never write AI/agent/IDE name in commit messages
- Never commit .env.local or .env.production files
- Never mark build passing without running npm run build

## Always do
- All styling via Tailwind utility classes — no inline styles
- All forms use React Hook Form + Zod with inline error messages
- All numbers/prices/dates use font-mono (IBM Plex Mono)
- All cards use 1px solid #111111 border + 3px 3px 0px #111111 shadow
- All API routes return proper HTTP status codes
- Use Lucide React icons only — never custom SVGs for icons

---

# DECISION LOG

| Date       | Decision                                              | Why                                                        |
|------------|-------------------------------------------------------|------------------------------------------------------------|
| 2026-05-10 | SQLite for local dev instead of PostgreSQL            | PostgreSQL not installed on dev machine, Docker not running|
| 2026-05-10 | Prisma 5 instead of Prisma 7                          | Prisma 7 removed url from schema.prisma, too breaking      |
| 2026-05-10 | Store enums as String in SQLite schema                | SQLite doesn't support Prisma enum types natively          |
| 2026-05-10 | Next.js 16.2.6 (via create-next-app)                  | Latest stable at time of install; plan said 14+ but 16 ok  |
| 2026-05-10 | Tailwind CSS v4 (CSS-based config via @theme)         | Installed by create-next-app; v4 uses CSS not tailwind.config.ts |
| 2026-05-10 | Deploy build id via NEXT_PUBLIC_APP_VERSION + DEPLOY_VERSION in CI | Unique production label per deploy; semver stays in package.json for releases |
| 2026-05-10 | City search DB-only; no GeoDB merge in `/api/cities/search` | Product requirement; seeded cities as destinations |
| 2026-05-10 | Firebase: `FIREBASE_WEB_*` + merge `.env.production` + `force-dynamic` auth pages | PM2/build empty `NEXT_PUBLIC_*`; static bootstrap was null |
| 2026-05-10 | `proxy.ts` allow `/api/config/*` without auth | Login page fetch must work while logged out |
| 2026-05-10 | Droplet deploy: `fetch` then `reset --hard` only (no checkout first) | Dirty server edits to tracked files blocked old flow → aborted deploy → 502 |
| 2026-05-11 | Serve uploads through authenticated API route instead of public static files | Direct memory image URLs now require a logged-in session and ownership/share/trip access |
| 2026-05-11 | Keep deploy clean from deleting `storage` and legacy `public/uploads` | User-uploaded files must survive `git clean` during server deploys |

# VALIDATION LOG

| Date       | Who/Agent    | Check                        | Result  | Notes                            |
|------------|--------------|------------------------------|---------|----------------------------------|
| 2026-05-10 | Cursor Agent | prisma migrate dev           | pass    | SQLite dev.db created            |
| 2026-05-10 | Cursor Agent | prisma seed                  | pass    | 57 cities, 295 activities seeded |
| 2026-05-10 | Cursor Agent | npm run dev                  | pass    | localhost:3000 ready in 628ms    |
| 2026-05-10 | Cursor Agent | npm run build                | pass    | 0 errors, 15 routes compiled     |
| 2026-05-11 | Codex        | npx prisma validate          | pass    | Local SQLite schema valid after memory sharing/quota fields |
| 2026-05-11 | Codex        | prisma validate production   | pass    | PostgreSQL schema includes Trip.shareMemories, MemoryShare, storageLimit |
| 2026-05-11 | Codex        | npx tsc --noEmit             | pass    | TypeScript clean after Explore/Memories/Profile/Admin updates |
| 2026-05-11 | Codex        | npm run lint                 | pass    | ESLint clean; Playwright intentionally skipped per owner request |
| 2026-05-11 | Codex        | npm run build                | pass    | Next.js production build passed; 39 static/dynamic routes generated |
| 2026-05-11 | Codex        | npm.cmd run db:generate      | pass    | Prisma client regenerated; storageLimit, shares, memoryShare present |
| 2026-05-11 | Codex        | npx.cmd tsc --noEmit         | pass    | No TypeScript errors after memories/upload/profile/image fixes |
| 2026-05-11 | Codex        | npm.cmd run build            | pass    | Production build passed; `/profile` and `/api/uploads/[...path]` included |

---

# BACKLOG

| # | Area     | Task                                              | Status   | Priority |
|---|----------|---------------------------------------------------|----------|----------|
| 1 | Server   | SSH + droplet access documented in LOCAL_OPERATIONS.md | done     | critical |
| 2 | Server   | Full server setup (Node, PostgreSQL, Nginx, PM2)  | done     | critical |
| 3 | App      | Dashboard page (/dashboard)                       | pending  | high     |
| 4 | App      | My Trips page (/trips)                            | pending  | high     |
| 5 | App      | Create Trip form (/trips/new)                     | pending  | high     |
| 6 | App      | Itinerary Builder (/trips/[id]/builder)           | pending  | high     |
| 7 | App      | Itinerary View (/trips/[id])                      | pending  | high     |
| 8 | App      | Budget page (/trips/[id]/budget)                  | pending  | high     |
| 9 | App      | City Search + detail (/cities, /cities/[id])      | pending  | medium   |
|10 | App      | Packing Checklist (/trips/[id]/packing)           | pending  | medium   |
|11 | App      | Trip Notes (/trips/[id]/notes)                    | pending  | medium   |
|12 | App      | Public Share view (/trip/[slug])                  | pending  | medium   |
|13 | App      | User Profile (/profile)                           | pending  | medium   |
|14 | App      | Admin Dashboard (/admin)                          | pending  | low      |
|15 | App      | Route Map (Leaflet) on trip view                  | pending  | low      |
|16 | Infra    | Rename middleware.ts to fix Next.js 16 warning    | done     | low      |
|17 | Infra    | Switch schema to PostgreSQL for production        | done     | pre-deploy |

---

# CONTEXT FILE LOG

| Date       | Changed by    | What changed in this file                          |
|------------|---------------|----------------------------------------------------|
| 2026-05-10 | Cursor Agent  | Created initial context from CONTEXT TAMPLATE.md   |
| 2026-05-10 | Cursor Agent  | Updated: build pass, GitHub push, SSH status, middleware rename |
| 2026-05-10 | Cursor Agent  | Deploy version pipeline, responsive shell tweaks, CONTEXT sync |
| 2026-05-10 | Antigravity   | Updated: server setup, CI/CD, subdomain, Firebase setup         |
| 2026-05-10 | Cursor Agent  | CONTEXT: handoff, FILE MAP, cities/search, Firebase/proxy, WORKING DIRECTORY lib + proxy |
| 2026-05-10 | Antigravity   | FIXED: Explore API 'any' types, missing imports, Image optimization, Profile Location card |
| 2026-05-10 | Antigravity   | FEATURE: Memories gallery (200MB quota, trip-sync, Google Photos style) |
| 2026-05-10 | Cursor Agent  | BUILD & DEPLOY: tips for 502 / dirty-git-on-droplet; NAVIGATION + DECISION LOG sync |
| 2026-05-10 | Antigravity   | STABILISATION: Fixed broken avatars, implemented trip deletion, redesigned Explore swiper, added collaborator support, fixed async params for Next.js 15+ |
| 2026-05-11 | Codex         | FIX/FEATURE: production schema drift guard, memory sharing without duplicates, storage quotas, custom city creation, Manali guide, maps/cost previews, Explore/Profile/Admin/Memories polish, security headers, nginx rate limits, faster deploy npm ci |
| 2026-05-11 | Codex         | SECURITY/FIX: private upload serving, profile/build verification, activity image fallback update |
